int atoi(char* input){

    int i = 0;
    int partialResult = 0;
    while ( input[i] != 0){
      int digit = input[i] - 48;
      partialResult = digit + PartialResult * 10;
      ++i;
    }

    return patialResult;
}
